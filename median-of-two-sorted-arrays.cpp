#include <bits/stdc++.h>
using namespace std;
class Solution {
public:
    int smallerOdd(int len){
        return ceil(float(len)/2);
    } 
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
       int len = nums1.size() + nums2.size();
       if (len % 2 != 0){

       }
       else{

       }
    }
};